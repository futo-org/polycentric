import React from 'react'
import ReactDOM from 'react-dom/client'
import PolycentricReact from "@polycentric/polycentric-react"

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <PolycentricReact />
  </React.StrictMode>,
)
