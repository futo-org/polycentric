// Verifications tab, claim creation and the claim screen.
output.verifications = {
  // Accessibility label of the sidebar item (web); tab label on native.
  nav: 'Verifications',
  // Topbar button and empty-state button share this label.
  createClaim: 'Create a claim',
  skillType: 'Skill',
  create: 'Create',
  claimCreated: 'Claim created',
  // Button on the claim screen; also the title of the sheet it opens.
  requestVerification: 'Request verification',
  // Accessibility label of the search input in that sheet.
  searchProfiles: 'Search profiles',
  verificationRequested: 'Verification requested',
  // Heading above the verifier rows; gone when there are none.
  verifiers: 'Verifiers',
  requested: 'Requested',
  // Accessibility label of the dots button on a verifier row.
  verifierOptions: 'Verifier options',
  // Menu item; also the confirm button's title of the alert it opens.
  cancelRequest: 'Cancel request',
  cancelRequestTitle: 'Cancel request?',
  notVerified: 'Not verified',
};
